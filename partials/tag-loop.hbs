<section class="loop tag-loop" data-column="4">
    {{#foreach posts}}
    <article class="{{post_class}}">
        <a href="{{url}}">
            <img src={{image}} alt="{{title}}"/>
        </a>
    </article>
    {{/foreach}}
    {{#foreach posts}}
    <article class="{{post_class}}">
        <a href="{{url}}">
            <img src={{image}} alt="{{title}}"/>
        </a>
    </article>
    {{/foreach}}
</section>


{{!-- {{#foreach posts}}
<article class="{{post_class}}">
    <header class="post-list-header">
            <a href="{{url}}">
                <img src={{image}} alt="{{title}}"/>
            </a>
    </header>
    
    <h2 class="post-list-title"><a href="{{url}}">{{title}}</a></h2>
</article>
{{/foreach}} --}}

{{!-- Previous/next page links - displayed on every page --}}
{{!-- pagination --}}